# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# The Read the Docs conf file version (required).
version: 2

# Install the project package prior to build.
python:
  install:
    - method: pip
      path: .
